# Copy this file to:
#   android-app/gradle-local.properties
#
# Then replace the sample values with your own backend endpoint.
# This file is ignored by git, so each developer can keep a private local config.

studentclockinSupabaseUrl=https://your-api.example.com
studentclockinSupabasePublicKey=student-checkin-public

# Legacy key name is also supported if older scripts still use it:
# studentclockinSupabaseAnonKey=student-checkin-public
